/* ═══════════════════════════════════════════════════════
   ScanLine.jsx — Ligne de balayage "NOC" (CSS pur)
   Fine ligne cyan qui descend en boucle du haut vers le bas.
   Styles + animation dans globals.css (.scan-line).
   ═══════════════════════════════════════════════════════ */
export default function ScanLine() {
  return <div className="scan-line" aria-hidden="true" />
}
